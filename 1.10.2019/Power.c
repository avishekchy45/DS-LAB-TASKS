#include<stdio.h>
#include<math.h>

main()
{
    int n,k=0;
    scanf("%d",&n);
    int arr[1000],s[1000];
    for(int i=0; i<n; i++)
    {
        s[]=pow(10,i);
        for(int j=0; j<n; j++,k++)
    arr[k]=s[j];
    }
    for(int i=n; i<2n; i++)
    {
        printf("%d",arr[i]);
    }
}
